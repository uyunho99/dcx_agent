# DCX Agent — 소비자 인사이트 분석 파이프라인

## 개요

DCX Agent는 **네이버 카페 기반 소비자 인사이트 분석 플랫폼**입니다. 사용자가 분석하고 싶은 제품/서비스를 입력하면, AI 키워드 생성 → 대량 크롤링 → 데이터 정제 → ML 분류 → 클러스터링 → 페르소나 도출까지 **8단계 파이프라인**을 통해 소비자 인사이트를 자동으로 추출합니다.

### 시스템 구성

```
사용자 브라우저
      │
   Nginx (:80)  ← 리버스 프록시
      │
  ┌───┴───┐
  │       │
Next.js  FastAPI
(:3000)  (:8000)
  │       │
  │    ┌──┴──────────────────────────┐
  │    │  Claude API   (키워드/페르소나)  │
  │    │  Naver Cafe API (크롤링)       │
  │    │  Voyage AI     (임베딩)        │
  │    │  Pinecone      (벡터 검색)     │
  │    │  AWS S3        (데이터 저장)    │
  │    └─────────────────────────────┘
```

### 전체 플로우 요약

```
START → KEYWORDS → CRAWLING → PREPROCESSING → LABELING → TRAINING → CLUSTERING → PERSONAS
  │                                                                                  │
  │    ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←  │
  │                                                                                  │
  └──── 세션 저장/복원, RAG Chat, Insight Chat, SNA 시각화 ──────────────────────────────┘
```

---

## 공통 UI 구성

모든 파이프라인 페이지는 동일한 레이아웃을 공유합니다.

### 상단바 (TopBar)
- DCX 로고
- 현재 분석 중인 **제품 키워드**와 **세션 ID** 표시
- 세션이 없으면 "세션 없음" 표시

### 좌측 사이드바 (StepBar)
- 8단계 진행 표시기
- 각 단계별 상태: 완료(✓), 현재(하이라이트), 미진행(회색)
- 상단에 전체 진행률 프로그레스 바
- 각 단계 클릭 시 해당 페이지로 이동 가능

### 우측 채팅 패널 (ChatPanel)
- 💬 버튼으로 열고 닫기
- 파이프라인 진행 상황이나 결과에 대해 AI에게 질문 가능
- 현재 단계, 제품명, 키워드 수 등의 컨텍스트를 자동으로 포함하여 질의
- Pinecone RAG 기반으로 수집된 실제 문서를 참조하여 답변

### 세션 관리
- 페이지 로드 시 이전 세션을 자동 복원
- 모든 데이터는 S3에 영구 저장
- 최대 10개의 이전 세션 목록 표시 및 빠른 로드 지원

---

## Stage 1. Start — 세션 생성

> **목적**: 분석 대상 제품과 타겟 소비자를 정의하여 새 분석 세션을 생성합니다.

### 사용자가 보는 화면

#### 이전 세션 목록
- 저장된 세션 최대 10개가 카드 형태로 표시
- 각 카드에 **제품 키워드**, **현재 진행 단계**, **세션 ID** 표시
- 클릭하면 해당 세션의 마지막 진행 단계로 바로 이동
- 각 세션별 삭제 버튼 제공

#### 새 세션 생성 폼
| 입력 항목 | 형태 | 예시 |
|-----------|------|------|
| **메인 키워드** | 텍스트 입력 | "에어컨" |
| **문제 정의** | 텍스트영역 (3줄) | "에어컨 구매 시 소비자 고민 파악" |
| **타겟 인구통계** | 클릭 태그 (복수 선택) | 주부, 직장인, 1인가구, 신혼부부, 육아맘, 시니어, 대학생, 자영업 |
| **연령대** | 클릭 태그 (복수 선택) | 20대, 30대, 40대, 50대+ |
| **성별** | 클릭 태그 (복수 선택) | 남성, 여성 |

### 사용자 액션
1. 태그를 클릭하여 타겟 인구통계, 연령대, 성별을 선택 (토글 방식)
2. 메인 키워드와 문제 정의를 입력
3. **"시작"** 버튼 클릭

### 처리 과정
- 고유 세션 ID 생성 (형식: `s` + 타임스탬프)
- 세션 정보를 S3에 저장
- 키워드 생성 단계로 자동 이동

---

## Stage 2. Keywords — AI 키워드 생성

> **목적**: Claude AI가 네이버 카페 검색에 최적화된 키워드를 4라운드에 걸쳐 생성하고, 사용자가 직접 검토/수정합니다.

### 라운드 구조

| 라운드 | 이름 | 목표 |
|--------|------|------|
| **R1** | 발산 | 70개 이상의 다양한 초기 키워드 생성 |
| **R2** | 발산 | 기존 키워드를 보고 새로운 관점의 키워드 추가 |
| **R3** | 수렴 | 핵심 키워드로 수렴, 160개 이하로 정리 |
| **R4** | 재발산 | 160개 초과 시 확장 라운드 진행 |
| **Final** | 최종 확정 | 모든 키워드 최종 승인/거절 |

### 사용자가 보는 화면

#### 라운드 진행 시 (R1~R4)
- 상단에 **라운드 표시기**: "R1: 발산", "R2: 발산", "R3: 수렴" 등
- **누적 배지**: "누적: X개 | 신규: Y개"
- **카테고리별 섹션** (접기/펼치기 가능):
  - 각 카테고리 안에 키워드 태그 나열
  - 접힌 상태에서 "기존 X + 신규 Y" 요약 표시

#### 키워드 태그 색상 구분
| 상태 | 색상 | 의미 |
|------|------|------|
| 기존 (old) | 회색 | 이전 라운드에서 승인된 키워드 |
| 신규 (new) | 인디고 | AI가 새로 생성한 키워드 (점수 표시) |
| 승인 (approved) | 인디고 | 사용자가 승인한 키워드 |
| 거절 (rejected) | 회색 + 취소선 | 사용자가 거절한 키워드 |
| 수동 추가 (manual) | 에메랄드 | 사용자가 직접 입력한 키워드 |
| 추천 (suggested) | 바이올렛 | AI 추천 단어에서 추가한 키워드 |

#### 키워드 점수 체계
각 키워드는 네이버 카페 API로 실제 검색 결과 수를 조회하여 점수를 매깁니다:

| 검색 결과 수 | 점수 |
|-------------|------|
| 10,000건 이상 | 95점 |
| 5,000 ~ 9,999건 | 85점 |
| 1,000 ~ 4,999건 | 75점 |
| 100 ~ 999건 | 55점 |
| 10 ~ 49건 | 35점 |
| 10건 미만 | 25점 |

#### 최종 확정 모드 (Final)
- 전체 누적 키워드를 한 번에 표시
- **"전체 승인"** / **"전체 거절"** 일괄 버튼
- 개별 키워드 토글로 세밀한 조정 가능

### 사용자 액션
| 액션 | 설명 |
|------|------|
| **키워드 토글** | 클릭하여 개별 승인/거절 |
| **드래그 앤 드롭** | 키워드를 다른 카테고리로 이동 |
| **카테고리 접기/펼치기** | 화살표 또는 카테고리명 클릭 |
| **수동 키워드 추가** | 입력 필드에 쉼표로 구분하여 입력 → "추가" |
| **AI 추천 단어 추가** | 형용사/동사 추천 목록에서 개별 또는 "전체 추가" |
| **새 카테고리 생성** | "+ 새 카테고리 추가" 클릭 |
| **빈 카테고리 삭제** | 빈 카테고리의 "삭제" 클릭 |
| **라운드 승인** | "승인 → 다음" 클릭하여 다음 라운드로 |
| **최종 확정** | "확정 → 크롤링" 클릭하여 크롤링 단계로 이동 |

### 처리 과정
1. Claude API에 제품명, 문제 정의, 기존 키워드를 전달
2. 라운드별로 다른 프롬프트 전략 적용 (R1: 다양성, R2: 신규성, R3: 수렴)
3. 생성된 키워드를 네이버 카페 API로 검색하여 점수 산출
4. 길이 2자 미만, 중복, 일반적 단어("후기", "비교", "추천" 등) 자동 필터링
5. 점수 기준 정렬 후 사용자에게 반환

---

## Stage 3. Crawling — 네이버 카페 크롤링

> **목적**: 확정된 키워드로 네이버 카페에서 소비자 게시글을 대량 수집합니다.

### 사용자가 보는 화면

#### 크롤링 설정 (시작 전)

| 설정 항목 | 형태 | 기본값 | 설명 |
|-----------|------|--------|------|
| **크롤링 모드** | 2개 버튼 선택 | - | API-Only (빠름) 또는 API + 본문 크롤링 (정밀) |
| **네이버 쿠키** | 텍스트영역 | - | 본문 크롤링 모드에서만 표시 (NID_AUT, NID_SES) |
| **목표 수집량** | 숫자 입력 | 50,000 | 수집할 문서 수 |
| **수집 기간** | 날짜 선택 (시작~끝) | - | 특정 기간의 게시글만 수집 |
| **포함 카페** | 쉼표 구분 텍스트 | - | 특정 카페만 수집 (화이트리스트) |
| **제외 카페** | 쉼표 구분 텍스트 | - | 특정 카페 제외 (블랙리스트) |
| **광고 필터** | 텍스트영역 | 기본 광고 키워드 | 제목/본문에 포함 시 제외할 키워드 |

#### 크롤링 모드 상세

| 모드 | 속도 | 수집 범위 | 쿠키 필요 |
|------|------|-----------|-----------|
| **API-Only** | 빠름 | 제목 + 요약(description) | 불필요 |
| **API + 본문 크롤링** | 느림 (정밀) | 제목 + 요약 + 본문 전문 + 조회수/댓글/좋아요 | 필요 |

#### 크롤링 진행 중
- **진행 상태 메시지**:
  - "1단계: API 수집중... X건"
  - "1단계: API 수집 완료 — X건 | 2단계: 본문 + 메타데이터 크롤링"
- **프로그레스 바**: 본문 크롤링 진행률
- **실시간 통계**: 성공 X건 (녹색), 실패 X건 (빨강), 총 X건 (회색)
- **"크롤링 중지"** 버튼으로 언제든 중단 가능

#### 크롤링 완료 후
- **수집 통계**: API 수집 건수, 본문 성공/실패 건수, 평균 본문 길이, 성공률
- **메타데이터 요약**: 평균 조회수, 댓글수, 좋아요수, 고유 작성자 수, 상위 게시판 분포
- **카페별 수집 현황**: 카페 이름별 수집 건수를 태그 형태로 표시

### 처리 과정

#### Phase 1: Naver API 수집
1. 각 키워드마다 `"{제품명} {키워드}"` 형태로 네이버 카페 API 검색
2. 키워드당 최대 1,000건, 한 번에 100건씩 페이지네이션
3. 순차 필터링: 날짜 범위 → 광고 필터 → 카페 제외/포함 → 중복 제거
4. S3에 500건 단위로 JSONL 파일 저장

#### Phase 2: 본문 크롤링 (선택)
1. Phase 1에서 수집한 URL 목록 로드
2. Crawl4AI + Playwright로 실제 게시글 페이지 방문
3. 본문, 조회수, 댓글수, 좋아요수, 게시판명 추출
4. Phase 1 데이터에 병합하여 재저장

### 사용자 액션
1. 크롤링 모드 선택
2. (본문 모드 시) 네이버 쿠키 입력
3. 목표 수집량, 기간, 카페 필터 설정
4. **"크롤링 시작"** 클릭
5. 진행 상황 모니터링 (3초마다 자동 갱신)
6. 완료 후 **"전처리 →"** 클릭

---

## Stage 4. Preprocessing — 데이터 전처리

> **목적**: 수집된 원시 데이터에서 광고, 중복, 노이즈를 제거하여 분석 가능한 데이터셋을 만듭니다.

### 사용자가 보는 화면

#### 전처리 설정 (시작 전)
- **추가 필터 키워드** 입력 필드 (텍스트영역)
  - 크롤링 시 사용한 광고 필터 외에 추가로 제외할 키워드 입력
- **"전처리 시작"** 버튼

#### 전처리 진행 중
- "처리 중..." 메시지와 스피너
- 프로그레스 바 (0~100%)

#### 전처리 완료
- **결과 표시**: "완료! 원본: X → 정제: Y건"
  - 원본 건수 대비 정제 후 건수를 한눈에 비교

### 처리 과정
1. 크롤링 데이터 전체 로드 (`crawl/{sid}/`)
2. 순차 필터링:
   - 광고 키워드 매칭 (제목 + 설명 + 본문 결합 텍스트)
   - 카페 제외 리스트 적용
   - 링크 기반 중복 제거
   - 최소 길이 검증: 제목 5자 미만 AND 설명 10자 미만인 문서 제외 (본문이 있으면 유지)
3. 각 문서에 순차 인덱스(`idx`) 부여
4. 정제된 데이터를 타임스탬프와 함께 S3에 저장

### 사용자 액션
1. (선택) 추가 필터 키워드 입력
2. **"전처리 시작"** 클릭
3. 완료 후 **"라벨링 →"** 클릭

---

## Stage 5. Labeling — 수동 라벨링

> **목적**: 전처리된 데이터의 2% 랜덤 샘플을 추출하여 사용자가 직접 관련/무관을 판단합니다. 이 라벨이 다음 단계 ML 학습의 학습 데이터가 됩니다.

### 사용자가 보는 화면

#### 진행 현황
- **목표 배지**: "목표: X건 | 완료: Y건"
- **프로그레스 바**: 라벨링 완료율 (목표 = 전체 문서의 2%)

#### 문서 카드 (배치 단위)
각 문서가 카드 형태로 표시됩니다:
- **제목** (굵은 텍스트)
- **설명/요약** (최대 3줄)
- **카페명** (파란색 배지)
- 하단에 두 개의 판단 버튼:
  - **"관련"** (녹색) — 분석 대상과 관련된 게시글
  - **"무관"** (빨강) — 광고, 관련 없는 게시글

#### 하단 컨트롤
- **"저장 후 다음"** (황색) — 현재 배치 라벨 저장 + 새 샘플 로드
- **"완료 → 학습"** (녹색) — 라벨링 종료 후 학습 단계로 이동

### 처리 과정
1. 전처리된 데이터에서 2% 랜덤 샘플 추출 (배치당 10~20건)
2. 사용자 라벨 수집: 관련(1) 또는 무관(0)
3. 라벨링된 데이터를 세션에 저장
4. 최소 5건 이상의 라벨 필요

### 사용자 액션
1. 각 문서를 읽고 **"관련"** 또는 **"무관"** 클릭
2. **"저장 후 다음"**으로 추가 샘플 라벨링
3. 충분히 라벨링했으면 **"완료 → 학습"** 클릭

---

## Stage 6. Training — ML 앙상블 학습

> **목적**: 사용자가 라벨링한 소량의 데이터로 3개 모델을 학습시킨 뒤, 전체 데이터를 자동으로 관련/무관 분류합니다.

### 사용자가 보는 화면

#### 학습 설정 (시작 전)
- **방법론 배지**: "3중 준지도 + LLM 앙상블"
- **모델 그리드** (4장의 카드): LSTM, CNN, GRU, Claude (LLM)
- **"학습 시작"** 버튼

#### 학습 진행 중
- **진행 상태**: "진행중... (phase)" + 스피너
  - Phase 표시: 초기화 → 데이터 준비 → LSTM 학습 → CNN 학습 → GRU 학습 → 분류 중 → 완료
- **프로그레스 바**: 0~100%

#### 학습 완료
- **결과 표시**: "완료! 전체: X, 관련: Y, 무관: Z"

### 앙상블 학습 상세

#### TensorFlow 경로 (기본)
| 모델 | 구조 |
|------|------|
| **LSTM** | Embedding(10K, 128) → LSTM(64) → Dense(32) → Dropout(0.5) → Dense(1, sigmoid) |
| **CNN** | Embedding(10K, 128) → Conv1D(64, k=5) → GlobalMaxPool → Dense(32) → Dropout(0.5) → Dense(1, sigmoid) |
| **GRU** | Embedding(10K, 128) → GRU(64) → Dense(32) → Dropout(0.5) → Dense(1, sigmoid) |

#### scikit-learn 경로 (폴백)
| 모델 | 설명 |
|------|------|
| **Logistic Regression** | TF-IDF(5000 features) + max_iter=500 |
| **Random Forest** | 100 estimators |
| **Gradient Boosting** | 100 estimators |

#### 앙상블 방식
- 3개 모델의 예측 확률을 **평균**
- 임계값 0.5 이상 → **관련(relevant)**
- 임계값 0.5 미만 → **무관(irrelevant)**

### 사용자 액션
1. **"학습 시작"** 클릭
2. 진행 상황 모니터링 (5초마다 자동 갱신)
3. 완료 후 **"클러스터링 →"** 클릭

### 출력 데이터
- `classified/{sid}/relevant_{timestamp}.jsonl` — 관련 문서 (relevance_score 포함)
- `classified/{sid}/irrelevant_{timestamp}.jsonl` — 무관 문서

---

## Stage 7. Clustering — 의미 기반 클러스터링

> **목적**: 관련 문서들을 의미적으로 유사한 그룹으로 자동 분류하고, 사용자가 클러스터를 정제합니다.

### 사용자가 보는 화면

#### 클러스터링 설정 (시작 전)
- **방법론 배지**: "TF-IDF + K-means + Ward"
- **"시작"** 버튼

#### 클러스터링 결과 (완료 후)
- **상태 배지**: "완료! X개"
- **클러스터 카드 그리드** (2열):
  - 각 카드에 **체크박스** (유지/제거 선택)
  - **클러스터 번호**: "클러스터 N"
  - **문서 수**: "(X건)"
  - **상위 5개 키워드** (쉼표 구분)
- **병합 입력 필드**: 병합할 클러스터 번호 입력 (예: "1,3,5")
- **"정제 → 페르소나"** 버튼

### 처리 과정
1. 관련 문서를 **Voyage AI**로 임베딩 (1024차원, 배치 128건, 텍스트 2000자 제한)
2. **최적 K 자동 선택**:
   - K = 3 ~ min(15, 데이터수/10) 범위에서 실루엣 점수 기준으로 최적 K 결정
3. **K-means 클러스터링** (n_init=10)
4. **TF-IDF 키워드 추출**: 클러스터별 상위 10개 키워드 (unigram + bigram, 최대 3000 features)
5. 각 문서에 클러스터 번호 할당

### 클러스터 정제 기능
사용자가 결과를 검토한 뒤:
- **유지할 클러스터 선택**: 체크박스로 불필요한 클러스터 제거
- **클러스터 병합**: 유사한 클러스터를 하나로 합침 (첫 번째 ID가 타겟)
- 정제 후 클러스터 번호 재정렬 (0, 1, 2, ...) 및 TF-IDF 키워드 재계산

### 사용자 액션
1. **"시작"** 클릭
2. 결과 클러스터 검토
3. 불필요한 클러스터 체크 해제
4. (선택) 병합할 클러스터 번호 입력
5. **"정제 → 페르소나"** 클릭

---

## Stage 8. Personas — 소비자 페르소나 도출

> **목적**: 각 클러스터에서 Claude AI가 소비자 페르소나를 자동 생성합니다. 페르소나에는 상황, 페인포인트, 인사이트가 포함됩니다.

### 사용자가 보는 화면

#### 페르소나 도출 전
- **안내 배지**: "Claude가 클러스터별 페르소나를 도출합니다."
- **보조 텍스트**: "임베딩 + 페르소나 도출이 자동 진행됩니다"
- **"페르소나 도출 시작"** 버튼

#### 페르소나 도출 중
- "Claude 분석중... X%" 메시지
- 프로그레스 바 (0~100%)

#### 페르소나 결과 (완료 후)
- **상태 배지**: "완료! X개 클러스터"
- 클러스터별 섹션:
  - **클러스터 헤더** (그래디언트 배경): "클러스터 X" 또는 커스텀 이름
  - **페르소나 카드** (1~2열 반응형 그리드):

```
┌─────────────────────────────────────┐
│  페르소나 이름 (인디고, 굵은 텍스트)      │
│                                     │
│  ┌─ 상황 (파란 테두리) ──────────────┐  │
│  │ 이 소비자가 처한 상황 설명         │  │
│  └────────────────────────────────┘  │
│                                     │
│  ┌─ Pain Point (로즈 테두리) ────────┐  │
│  │ 핵심 고민/불만 사항               │  │
│  └────────────────────────────────┘  │
│                                     │
│  ┌─ 인사이트 (에메랄드 배경) ─────────┐  │
│  │ 마케팅/제품 개선에 활용할 인사이트   │  │
│  └────────────────────────────────┘  │
└─────────────────────────────────────┘
```

### 페르소나 수 결정 로직

| 클러스터 문서 수 | 기본 페르소나 수 | 키워드 다양성 높으면 |
|-----------------|-----------------|-------------------|
| 50건 미만 | 1개 | +1개 |
| 50 ~ 200건 | 2개 | +1개 |
| 200건 이상 | 3개 | +1개 |

### 처리 과정
1. 클러스터별 문서 매핑 로드
2. Pinecone RAG로 각 클러스터의 대표 샘플 검색
3. 클러스터 크기와 키워드 다양성에 따라 페르소나 수 결정
4. Claude API에 샘플 문서, 키워드, 제품 정보를 전달
5. JSON 형태로 페르소나 생성: 이름, 상황, 페인포인트, 인사이트

### 사용자 액션
1. **"페르소나 도출 시작"** 클릭
2. 진행 상황 모니터링
3. 완료 후 결과 확인
4. **"처음으로"** 버튼으로 시작 페이지 복귀

---

## 부가 기능

### RAG Chat — 문서 기반 AI 질의응답

> 수집된 소비자 게시글을 벡터 검색하여 AI가 근거 기반으로 답변합니다.

- **작동 방식**:
  1. 사용자 질문을 Voyage AI로 임베딩
  2. Pinecone에서 유사도 상위 5개 문서 검색
  3. 검색된 문서 + 페르소나 + 클러스터 정보를 컨텍스트로 포함
  4. Claude가 근거 기반 답변 생성
- **특수 기능**: 답변 중 새로운 키워드가 발견되면 `ADDED_KEYWORDS`로 추출하여 반환
- **접근 방법**: 우측 💬 채팅 패널에서 언제든 사용 가능

### Insight Chat — 페르소나/클러스터 수정

> 대화형으로 페르소나와 클러스터를 수정할 수 있습니다.

- **분석 모드**: 인사이트에 대한 질문 → AI가 분석/추천 제공
- **수정 모드**: 클러스터 병합, 페르소나 이름 변경, 페인포인트 수정 등의 명령
  - 수정 사항이 감지되면 자동으로 S3 데이터 업데이트
  - 수정 여부를 `modified: true/false`로 응답

### SNA 시각화 — 소셜 네트워크 분석 그래프

> 제품 → 클러스터 → 페르소나 관계를 D3.js 네트워크 그래프로 시각화합니다.

```
           ┌── 페르소나 A (pain_point, insight)
    클러스터 1 ─┤
           └── 페르소나 B (pain_point, insight)
   /
제품명
   \
           ┌── 페르소나 C (pain_point, insight)
    클러스터 2 ─┤
           └── 페르소나 D (pain_point, insight)
```

- **노드 유형**:
  - `product` (중심, 크기 40): 분석 대상 제품
  - `cluster` (중간, 크기 20): 소비자 클러스터
  - `persona` (외곽, 크기 12): 개별 페르소나 (페인포인트 + 인사이트 포함)
- **링크**: 클러스터 문서 수에 비례하는 두께

### 문서 검색

> 키워드로 수집된 문서를 의미 기반 검색합니다.

- **입력**: 검색 쿼리 텍스트 + 반환 건수 (기본 10건)
- **처리**: Voyage AI 임베딩 → Pinecone 유사도 검색
- **결과**: 유사도 점수, 제목, 설명, 키워드, 카페명, 클러스터 번호

---

## 비동기 작업 관리

시간이 오래 걸리는 작업은 백그라운드 데몬 스레드로 실행되며, 프론트엔드에서 주기적으로 상태를 폴링합니다.

### 비동기 작업 목록

| 작업 | 폴링 주기 | 중지 가능 |
|------|-----------|-----------|
| 크롤링 | 3초 | O |
| 전처리 | 3초 | X |
| 학습 | 5초 | X |
| 클러스터링 | 3초 | X |
| 임베딩 | 3초 | X |
| 페르소나 생성 | 3초 | X |

### 상태 값

| 상태 | 의미 |
|------|------|
| `running` | 작업 진행 중 |
| `done` | 작업 완료 |
| `error` | 오류 발생 |
| `not_found` | 작업 없음 (S3 폴백 확인) |

### 작업 관리자 (JobManager)
- 인메모리 싱글톤으로 모든 비동기 작업 상태 관리
- `threading.Lock()`으로 스레드 안전성 보장
- 작업 상태가 만료되면 S3에서 결과를 폴백으로 조회

---

## 데이터 저장 구조

모든 데이터는 AWS S3에 저장되며, 파이프라인 단계별로 경로가 구분됩니다.

```
sessions/{sid}/session.json              ← 세션 메타데이터, 키워드, 라벨
crawl/{sid}/*.jsonl                      ← 크롤링 원시 데이터 (500건 단위 배치)
preprocessed/{sid}/{timestamp}.jsonl     ← 전처리된 문서
classified/{sid}/relevant_{ts}.jsonl     ← ML 관련 판정 문서
classified/{sid}/irrelevant_{ts}.jsonl   ← ML 무관 판정 문서
clusters/{sid}/cluster_{id}_{ts}.jsonl   ← 초기 클러스터
clusters_refined/{sid}/data_{ts}.jsonl   ← 정제된 클러스터
personas/{sid}/result_{ts}.json          ← 페르소나 결과
```

| 형식 | 용도 |
|------|------|
| **JSONL** | 대량 문서 데이터 (한 줄에 하나의 JSON 객체) |
| **JSON** | 메타데이터, 페르소나 결과 등 구조화된 데이터 |
